# Docker Installation Notes on Kali Linux

## Steps
1. Updated system packages using "sudo apt update && sudo apt upgrade -y"
2. Installed docker.io via apt
3. Enabled and started docker service
4. Added user (kali) to docker group to avoid sudo
5. Simple docker run test using image Hello World
5. Installed docker-compose
